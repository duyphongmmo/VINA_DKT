
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div>
    <nav class="navbar navbar-expand navbar-light bg-white topbar mb-4 static-top shadow">
        <!-- Sidebar Toggle (Topbar) -->
        <!-- Topbar Navbar -->
        <ul class="navbar-nav ml-auto">
            <li class="nav-item dropdown no-arrow mx-1">
                <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <span class="mr-2 d-none d-lg-inline text-gray-600 small">Language</span>

                </a>
                <!-- Dropdown - User Information -->
                <div class="dropdown-menu dropdown-menu-right shadow animated--grow-in" aria-labelledby="userDropdown">

                    <a class="dropdown-item" href="#" style="text-align:center" onclick="location.href='@Url.Action("Index","HomeVN")'">
                        Viet Nam

                    </a>
                    <a class="dropdown-item" href="#" style="text-align:center" onclick="location.href='@Url.Action("Index","HomeKR")'">
                        Korea

                    </a>

                </div>
            </li>
            <div class="topbar-divider d-none d-sm-block"></div>
            <li class="nav-item dropdown no-arrow">
                <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <span class="mr-2 d-none d-lg-inline text-gray-600 small">

                        Mật khẩu mặc định
                    </span>

                </a>
                <!-- Dropdown - User Information -->
                <div class="dropdown-menu dropdown-menu-right shadow animated--grow-in" aria-labelledby="userDropdown">
                    &nbsp &nbsp &nbsp
                    &nbsp<i class="fas fa-list fa-sm fa-fw mr-2 text-gray-400"></i>
                    @Html.ActionLink("Đặt lại", "Reset_Password", "Home")
                    <script type="text/javascript">

                                    function preventBack()
                                    {
                                        window.history.forward();
                                    }
                                    setTimeout("preventBack()", 0);
                                    window.onunload=function()
                                    {
                                        null;
                                    }
                    </script>
                </div>

            </li>
            <div class="topbar-divider d-none d-sm-block"></div>

            <li class="nav-item dropdown no-arrow">
                <a class="nav-link dropdown-toggle" href="#" id="userDropdown" style="color:cadetblue" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <span class="mr-2 d-none d-lg-inline text-gray-600 small">
                        @if (Session["Name"] != null)
                        {
                            @Session["Name"].ToString()
                        }
                        else
                        {
                            Response.Redirect("Index");
                        }

                    </span>

                </a>
                <!-- Dropdown - User Information -->

            </li>
            <!-- Nav Item - Search Dropdown (Visible Only XS) -->
           
            <div class="topbar-divider d-none d-sm-block"></div>
            <!-- Nav Item - User Information -->
            <li class="nav-item dropdown no-arrow ">
                <a class="nav-link dropdown-toggle" href="#" id="userDropdown" style="color:cadetblue" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <span class="mr-2 d-none d-lg-inline text-gray-600 small">
                        @if (Session["USER_CODE"] != null)
                        {
                            @Session["USER_CODE"].ToString()
                        }
                        else
                        {
                            Response.Redirect("Index");
                        }

                    </span>

                </a>
                <!-- Dropdown - User Information -->
                <div class="dropdown-menu dropdown-menu-right shadow animated--grow-in" aria-labelledby="userDropdown">
                    <a class="dropdown-item" onclick="location.href='@Url.Action("IndexImage","Home")'">
                        <i class="fas fa-cogs fa-sm fa-fw mr-2 text-gray-400"></i>
                        Create Notification
                    </a>
                    <div class="dropdown-divider"></div>

                    <a class="dropdown-item" onclick="location.href='@Url.Action("Index","UserLogin")'" data-toggle="modal" data-target="#logoutModal">
                        <i class="fas fa-sign-out-alt fa-sm fa-fw mr-2 text-gray-400"></i>
                        Logout
                        <script type="text/javascript">

                                   function preventBack()
                                   {
                                       window.history.forward();
                                   }
                                   setTimeout("preventBack()", 0);
                                   window.onunload=function()
                                   {
                                       null;
                                   }
                        </script>
                    </a>

                </div>
            </li>
        </ul>
    </nav>
    <div class="container-fluid" style="padding-left:0.5rem;padding-right:0.5rem">
        <img src="~/AssetsLogin/Login/images/Backgrup.jpg" style="width: 100%; min-height: 100vh;
  display: -webkit-box;
  display: -webkit-flex;
  display: -moz-box;
  display: -ms-flexbox;
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  align-items: center; " />
    </div>
</div>

